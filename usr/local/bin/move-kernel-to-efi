#!/bin/bash

echo "move to kernel temporary disabled"
# Destination directory
# DEST="/efi/EFI/arch"

# Ensure destination directory exists
# mkdir -p "${DEST}"

# Copy files
# cp /boot/* "${DEST}/"

# echo "Files copied to ${DEST}"
